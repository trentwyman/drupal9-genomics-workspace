{#
/**
 * @file
 * Theme override for an Icon media item.
 *
 * All classes are prefixed with 'media--' to avoid collisions with other icon
 * classes.
 *
 * Available variables:
 * - media: The media item, with limited access to object properties and
 *   methods. Only method names starting with "get", "has", or "is" and
 *   a few common methods such as "id", "label", and "bundle" are available.
 *   For example:
 *   - entity.getEntityTypeId() will return the entity type ID.
 *   - entity.hasField('field_example') returns TRUE if the entity includes
 *     field_example. (This does not indicate the presence of a value in this
 *     field.)
 *   Calling other methods, such as entity.delete(), will result in
 *   an exception.
 *   See \Drupal\Core\Entity\EntityInterface for a full list of methods.
 * - name: Name of the media item.
 * - content: Media content.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 * - view_mode: View mode; for example, "teaser" or "full".
 * - attributes: HTML attributes for the containing element.
 * - title_attributes: Same as attributes, except applied to the main title
 *   tag that appears in the template.
 *
 * @see template_preprocess_media()
 *
 * @ingroup themeable
 */
#}
{% set image = content.icon_image.0 %}
{% set image = image|merge({'#item_attributes': {'class': ['media--icon__image', 'nal-image', 'nal-image--icon']}}) %}
<div{{ attributes.addClass('media--icon') }}>
  {{ title_suffix.contextual_links }}
  <a class="media--icon__link" href="{{ content.icon_link.0 }}" rel="bookmark">
    {{ image }}
    {{ title_prefix }}
      <span class="media--icon__title">{{ name }}</span>
    {{ title_suffix|without('contextual_links') }}
  </a>
  {% if content.icon_body.0 %}
    <div class="media--icon__body">{{ content.icon_body.0 }}</div>
  {% endif %}
</div>

